<h1>Editar Nota </h1>

<div>
    <% if @note.errors.any? %>
        <div>
            <ul>
                <% @note.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%= form_for(@note) do |f| %>
        <%= f.label :title, "Título" %>
        <%= f.text_field :title, class: "form-control" %>
        <br/>

        <%= f.text_area :content, class: "form-control" %>
        <br/>

        <%= f.submit "Guardar" %>
    <% end %>
</div>

<%= link_to "Cancelar", note_path(@note) %>
